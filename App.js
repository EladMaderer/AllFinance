import React from 'react';
import {SafeAreaView, View, Text, StyleSheet} from 'react-native';

const App = () => {
  return (
    <SafeAreaView style={{backgroundColor: 'red', flex: 1}}>
      <View>
        <Text>Clean sheet</Text>
      </View>
    </SafeAreaView>
  );
};

// const styles = StyleSheet.create({
//
// });

export default App;
